[tool.poetry]
name = "dairy"
version = "0.1.0"
description = "dairy"
authors = ["wumpscut0"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
Django = "^5.0.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
